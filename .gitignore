build/
.caustic/
# Compiler output, and only compiler output: `caustic -c foo.cst` always emits
# `foo.cst.s`, so that suffix is the exact discriminator.
*.cst.s
*.cst.s.o

# Python bytecode from the generators in tools/.
__pycache__/
*.pyc
